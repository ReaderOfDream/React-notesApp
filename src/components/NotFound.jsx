import React from 'react';
import styled from 'styled-components';

const NotFound = () => (<Content><h1>Directory not found :(</h1></Content>);

const Content = styled.div`
    margin: auto;
    text-align: center;
`;

export default NotFound;
